# Easter eggs — owner reference

This document intentionally reveals the hidden layer.

| Discovery | Trigger | Effect |
| --- | --- | --- |
| Graveyard | `cd ~/lab/.graveyard`, then `ls -a` / `cat` | Unlocks Archaeologist and exposes lessons from archived ideas |
| Local services | `nmap localhost`, then `nmap 1337` | Reveals fictional services and unlocks Curious |
| Snapshot panic | `rm -rf /` | Runs a brief harmless glitch and confirms snapshot restoration |
| Sudo boundary | `sudo su` or `root` | Explains that no real privilege/authentication exists |
| Build fuel | `coffee` | Restarts fictional focus.service |
| Restraint check | `matrix` | Declines the Matrix cliché and suggests a theme |
| Engineer fortune | `fortune` | Prints a failure-design aphorism |
| Important answer | `42` | Returns an interface-contract joke |
| Service log | `journalctl` | Lists fictional service state |
| Build target | `make portfolio` | Runs a short no-delay fake build |

Achievements also unlock for a first command, ten commands, mounting a project host,
and triggering snapshot recovery. Nothing here performs network scans, deletes data,
changes authentication, or executes machine commands.
