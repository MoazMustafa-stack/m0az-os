---
tags: [changelog, sessions]
---

# Change ledger

## 2026-08-28 — Repository foundation

- Initialized a current Next.js App Router project in `~/Desktop/Projects/m0az-os`.
- Recorded the repository audit and phased architecture plan.
- Added this Obsidian-compatible brain and future-session protocol.
- Added public-repository safety rules and gitignored private brain space.

Verification: dependency installation completed with zero reported vulnerabilities.

## 2026-08-28 — Product shell and public routes

- Added a restrained monochrome design system and responsive desktop/mobile shell.
- Added structured, publication-safe content for projects, research, skills,
  experience, lab, résumé, and contact views.
- Added static section routes and three generated project dossier routes.
- Added semantic error states, keyboard focus treatment, reduced-motion support,
  and a visible terminal dock ready for the command engine.

Verification: ESLint passed; Next.js production build generated 15 pages.

## 2026-08-28 — Terminal protocol and project hosts

- Added a quoted-input tokenizer, bounded command parser, typo suggestions, flags,
  aliases, and one-stage safe pipelines.
- Added a structured fictional filesystem with resolution, listing, reading, trees,
  project mounts, research files, and hidden graveyard entries.
- Implemented the documented navigation, filesystem, system, project-host, and
  contextual project commands plus a restrained hidden command layer.
- Added history navigation, Tab completion, Ctrl+L/C, terminal focus shortcut,
  versioned persistence, achievements, and a shared command palette.
- Kept all external openings restricted to curated content links; terminal input is
  never sent to a shell, evaluator, server, or dynamic endpoint.

Verification: ESLint, TypeScript, and the 15-page production build passed.

## 2026-08-28 — Release hardening and continuity

- Added first-visit boot, notification feedback, generated icon/social metadata,
  manifest, sitemap, robots directives, canonical URL handling, and public docs.
- Added parser, filesystem, command, and shell component tests plus desktop/mobile
  Playwright coverage for pointer navigation, terminal navigation, SSH/back state,
  screenshots, console errors, and framework overlays.
- Preserved mounted terminal sessions during shareable URL changes and synchronized
  browser back/forward navigation.
- Improved visible About/Resume access, mobile navigation scrolling, historical
  terminal prompts, and document-title updates.
- Added a least-privilege GitHub Actions workflow and removed unused starter assets.
- Rechecked tracked paths and credential signatures; private brain notes, env files,
  browser artifacts, and deployment state remain ignored.

Verification: dependency audit reported 0 vulnerabilities; lint and typecheck
passed; 14 unit/component tests passed; the production build generated 20 routes;
8 desktop/mobile browser tests passed with no representative console errors.
